name = "fluxo-de-caixa-backend"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Environment variables for Firebase
[vars]
FIREBASE_PROJECT_ID = "fluxo-de-caixa-pessoal-d6d3f"

# Secrets (set via wrangler secret put)
# FIREBASE_API_KEY - set via: wrangler secret put FIREBASE_API_KEY
# OPENAI_API_KEY - set via: wrangler secret put OPENAI_API_KEY

# CORS configuration - handled in code (src/index.ts)
